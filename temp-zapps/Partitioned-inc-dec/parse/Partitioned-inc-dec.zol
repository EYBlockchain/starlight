// SPDX-License-Identifier: CC0

pragma solidity ^0.8.0;

contract MyContract {

    secret uint256 private a;
    secret uint256 private b;

    function incr(secret uint256 param1) public {
        unknown a += param1;
        unknown b += param1;
    }

    function decr(secret uint256 param1) public {
        a -= param1;
        b += param1;
    }
}
