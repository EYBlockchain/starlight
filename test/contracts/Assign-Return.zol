// SPDX-License-Identifier: CC0

pragma solidity ^0.8.0;

contract Assign {

  secret uint256 private a;

  function add(secret uint256 value) public  returns (bool) {
    known a += value;
    return true;
  }

  function remove(secret uint256 value) public {
    a -= value;
  }
}
