// SPDX-License-Identifier: CC0

pragma solidity ^0.8.0;

contract Assign {

  secret uint256 private a;
  uint256 public b;
  uint256 public c;
  secret uint256 private result;
  function add(secret uint256 value) public {
    unknown a += value;
  result = (c > b? c: b);
  } 
}
