// SPDX-License-Identifier: CC0

pragma solidity ^0.8.0;

contract MyContract {

    secret uint256 private a;

    function incra(secret uint256 param3, uint256 param4) public {
        unknown a += param3 + param4 + 5;
    }
}
