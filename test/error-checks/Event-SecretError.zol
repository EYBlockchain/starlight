// SPDX-License-Identifier: CC0

pragma solidity ^0.8.0;

contract Assign {
  secret uint256 private a;
   event Deposit(uint256 _id);
   function deposit(secret uint256 _fid) public payable {     
     known a +=2; 
      emit Deposit(_fid);
   }
}
