// SPDX-License-Identifier: CC0

pragma solidity ^0.8.0;

contract knowfgfgn {

  secret uint256 private a;

  function secret (secret uint256 value) public {
    known a += value;
  }

  function secret(secret uint256 value, secret uint256 value2) public {
    known a += value2;
  }

  function unknown(secret uint256 value, secret uint256 known) public {
    known a += known;
  }

   function known(secret uint256 known2) public {
    known a += known2;
  }

}
